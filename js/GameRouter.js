const express = require('express');
const router = new express.Router();


router.get('/', function(req, res) {
  // serve the dashboard of the game here at the context root
});



module.exports = router;
